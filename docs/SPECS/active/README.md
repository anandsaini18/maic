# Active Specs

This directory contains implementation specs for work currently being built.

Rules:
- Keep specs focused and time-bounded.
- Move shipped specs to `../done/` when implementation lands and still matches shipped behavior.
- If a spec is superseded, move it to `../archive/` with an archive reason.
